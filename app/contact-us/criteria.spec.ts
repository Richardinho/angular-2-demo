import {} from 'jasmine';


let criteria, criteriaBuilder;

describe('builder', () => {

    it('should.', () => {
        expect(true).toBe(true);
    });

/*    beforeEach(() => {

        criteriaBuilder = new CriteriaBuilder();

        criteriaBuilder.download = Download.Epub;
        criteriaBuilder.filter = Filter.FreeEbooks;
        criteriaBuilder.startIndex = 8;
        criteriaBuilder.maxResults = 24;
        criteriaBuilder.printType = PrintType.Magazines;
        criteriaBuilder.projection = Projection.Lite;
        criteriaBuilder.orderBy = OrderBy.Newest;
        criteriaBuilder.title = 'my fave book';
        criteriaBuilder.author = 'Shakespeare';
        criteriaBuilder.publisher = 'cool books';
        criteriaBuilder.subject = 'interesting stuff';
        criteriaBuilder.isbn = 'isbn';
        criteriaBuilder.lccn = 'lccn';
        criteriaBuilder.oclc = 'oclc';

        criteria = criteriaBuilder.build();
    });

    it('should set properties on criteria', () => {

        expect(criteria.download).toBe(Download.Epub);
        expect(criteria.filter).toBe(Filter.FreeEbooks);
        expect(criteria.startIndex).toBe(8);
        expect(criteria.maxResults).toBe(24);
        expect(criteria.printType).toBe(PrintType.Magazines);
        expect(criteria.projection).toBe(Projection.Lite);
        expect(criteria.orderBy).toBe(OrderBy.Newest);
        expect(criteria.title).toBe('my fave book');
        expect(criteria.author).toBe('Shakespeare');
        expect(criteria.publisher).toBe('cool books');
        expect(criteria.subject).toBe('interesting stuff');
        expect(criteria.isbn).toBe('isbn');
        expect(criteria.lccn).toBe('lccn');
        expect(criteria.oclc).toBe('oclc');

    });*/
});

